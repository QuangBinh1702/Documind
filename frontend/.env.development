# Chỉ dùng cho `next dev` trên máy phát triển. Bản build production KHÔNG đặt
# biến này: gốc API rơi về chính origin của trang, và reverse proxy chuyển
# /api/* sang FastAPI — xem chú thích ở src/lib/api.ts.
NEXT_PUBLIC_API_URL=http://localhost:8000
